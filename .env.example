# Stays Booking API Configuration
# Base URL for Stays.net API
# Replace with your system's domain (e.g., casap.stays.net, app.stays.net)
VITE_STAYS_API_BASE_URL=https://casap.stays.net

# Your Stays.net API credentials
# Get these from your Stays.net account settings
VITE_STAYS_CLIENT_ID=your_client_id_here
VITE_STAYS_CLIENT_SECRET=your_client_secret_here

# Note: Booking API automatically retrieves all reservations for your account
# No need to specify individual listing IDs
