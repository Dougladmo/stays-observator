# Backend API Configuration (RECOMMENDED)
# Use this mode to connect to the centralized stays-api backend
VITE_API_BASE_URL=http://localhost:3001
VITE_API_KEY=your_api_key_here

# Legacy: Stays Booking API Configuration (deprecated)
# Only needed if NOT using the backend API
# VITE_STAYS_API_BASE_URL=https://casap.stays.net
# VITE_STAYS_CLIENT_ID=your_client_id_here
# VITE_STAYS_CLIENT_SECRET=your_client_secret_here
